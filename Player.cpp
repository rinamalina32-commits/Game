#include "Player.h"

Player::Player() {
    // Инициализация спрайта
}

void Player::update(float dt) {
    // Обновление позиции
}

void Player::render(sf::RenderTarget& target) {
    target.draw(sprite);
}
